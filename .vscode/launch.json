{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Fuzz Main",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/fuzzer.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
