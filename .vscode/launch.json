{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Fuzz Main",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/mutiprocess_main.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
